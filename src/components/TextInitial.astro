---
import TextWithYears from "./yearWrapper/TextWithYears";

interface Props {
  text: string;
}

const { text } = Astro.props;
const firstLetter = text.charAt(0);
const remainingText = text.slice(1);
---

<!-- <span class="inline leading-6">
  <span class="text-6xl pl-3 pr-1 pt-4 font-bold bg-brand-beige-dark text-black"
    >{firstLetter}</span
  >
  {remainingText}
</span> -->
<div class="pt-12">
  <span class="text-9xl -mt-12 text-brand-beige-dark inline float-start"
    >{firstLetter}</span
  >
  <TextWithYears body={remainingText} client:idle />
</div>
